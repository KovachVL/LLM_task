# LLM_task

How to start:

1. You need to download LM Studio and select the model you want to use.

2. In LM Studio's settings, you need to run this model for the API.

After that
``` pip install -r requirements.txt```
``` python app.py ```